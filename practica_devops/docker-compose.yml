version: '3.8'

services:
  mi_servidor_web:
    image: nginx:latest
    ports:
      - "8080:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html
    deploy:
      resources:
        limits:
          memory: 512M